# Skills CLI runtime (restored on session start)
.agents/

# User-local launcher overrides
claude.local.sh

# Template checksums (managed by update-from-template.sh)
.claude/.template-checksums

# MCP secrets (may contain API keys)
.mcp.env
