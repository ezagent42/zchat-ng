[project]
name = "zchat-acp"
version = "0.0.6.3"
description = "ZChat Agent Communication Protocol layer"
requires-python = ">=3.12"
dependencies = ["zchat-protocol"]

[tool.uv.sources]
zchat-protocol = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/zchat_acp"]
